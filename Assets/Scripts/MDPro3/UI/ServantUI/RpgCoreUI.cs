using MDPro3.UI.ServantUI;
using UnityEngine;

public class RpgCoreUI : ServantUI
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
